export enum AttackStrategy {
  RANDOM = 'RANDOM',
  WEAKEST = 'WEAKEST',
  STRONGEST = 'STRONGEST',
}
export enum BattleStatus {
  CREATED = 'created',
  STARTED = 'started',
  FINISHED = 'finished',
}
