export enum AttackStrategy {
  RANDOM = 'RANDOM',
  WEAKEST = 'WEAKEST',
  STRONGEST = 'STRONGEST',
}
export enum BattleStatus {
  CREATED = 'created',
  QUEUED = 'queued',
  STARTED = 'started',
  FINISHED = 'finished',
}
