import { BattleStatusType, StrategyType } from "./types"


export const BattleStatus: BattleStatusType[] = [
    'created', 'started', 'finished'
]
export const StrategyTypes: StrategyType[] = [
    'random', 'weakest', 'strongest'
]